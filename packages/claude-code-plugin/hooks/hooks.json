{
  "description": "Intercepts /magic-compact and creates a compacted destination session.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "matcher": "^/(?:claude-magic-compact:)?magic-compact(?:\\s+.*)?$",
        "hooks": [
          {
            "type": "command",
            "command": "bun ${CLAUDE_PLUGIN_ROOT}/src/hook.ts",
            "timeout": 150,
            "statusMessage": "Running Magic Compact"
          }
        ]
      }
    ]
  }
}
